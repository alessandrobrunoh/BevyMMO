// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::gather_session_type::GatherSession;
use super::vec_3_row_type::Vec3Row;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `gather_session`.
///
/// Obtain a handle from the [`GatherSessionTableAccess::gather_session`] method on [`super::RemoteTables`],
/// like `ctx.db.gather_session()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.gather_session().on_insert(...)`.
pub struct GatherSessionTableHandle<'ctx> {
    imp: __sdk::TableHandle<GatherSession>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

/// Lifetime-aware accessor marker for the table `gather_session`.
pub struct GatherSessionTableAccessor;

impl __sdk::TableAccessor<super::RemoteTables> for GatherSessionTableAccessor {
    type Row = GatherSession;
    type Handle<'db> = GatherSessionTableHandle<'db>;

    fn get<'db>(db: &'db super::RemoteTables) -> Self::Handle<'db> {
        db.gather_session()
    }
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `gather_session`.
///
/// Implemented for [`super::RemoteTables`].
pub trait GatherSessionTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`GatherSessionTableHandle`], which mediates access to the table `gather_session`.
    fn gather_session(&self) -> GatherSessionTableHandle<'_>;
}

impl GatherSessionTableAccess for super::RemoteTables {
    fn gather_session(&self) -> GatherSessionTableHandle<'_> {
        GatherSessionTableHandle {
            imp: self.imp.get_table::<GatherSession>("gather_session"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct GatherSessionInsertCallbackId(__sdk::CallbackId);
pub struct GatherSessionDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableLike for GatherSessionTableHandle<'ctx> {
    type Row = GatherSession;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = GatherSession> + '_ {
        self.imp.iter()
    }
}

impl<'ctx> __sdk::Table for GatherSessionTableHandle<'ctx> {
    type Row = GatherSession;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = GatherSession> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = GatherSessionInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GatherSessionInsertCallbackId {
        GatherSessionInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: GatherSessionInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = GatherSessionDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GatherSessionDeleteCallbackId {
        GatherSessionDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: GatherSessionDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

impl<'ctx> __sdk::WithInsert for GatherSessionTableHandle<'ctx> {
    type InsertCallbackId = GatherSessionInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GatherSessionInsertCallbackId {
        GatherSessionInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: GatherSessionInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }
}

impl<'ctx> __sdk::WithDelete for GatherSessionTableHandle<'ctx> {
    type DeleteCallbackId = GatherSessionDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GatherSessionDeleteCallbackId {
        GatherSessionDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: GatherSessionDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct GatherSessionUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for GatherSessionTableHandle<'ctx> {
    type UpdateCallbackId = GatherSessionUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> GatherSessionUpdateCallbackId {
        GatherSessionUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: GatherSessionUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

impl<'ctx> __sdk::WithUpdate for GatherSessionTableHandle<'ctx> {
    type UpdateCallbackId = GatherSessionUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> GatherSessionUpdateCallbackId {
        GatherSessionUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: GatherSessionUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

/// Access to the `entity_id` unique index on the table `gather_session`,
/// which allows point queries on the field of the same name
/// via the [`GatherSessionEntityIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.gather_session().entity_id().find(...)`.
pub struct GatherSessionEntityIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<GatherSession, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> GatherSessionTableHandle<'ctx> {
    /// Get a handle on the `entity_id` unique index on the table `gather_session`.
    pub fn entity_id(&self) -> GatherSessionEntityIdUnique<'ctx> {
        GatherSessionEntityIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("entity_id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> GatherSessionEntityIdUnique<'ctx> {
    /// Find the subscribed row whose `entity_id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<GatherSession> {
        self.imp.find(col_val)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<GatherSession>("gather_session");
    _table.add_unique_constraint::<u64>("entity_id", |row| &row.entity_id);
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::v2::TableUpdate,
) -> __sdk::Result<__sdk::TableUpdate<GatherSession>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<GatherSession>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

#[allow(non_camel_case_types)]
/// Extension trait for query builder access to the table `GatherSession`.
///
/// Implemented for [`__sdk::QueryTableAccessor`].
pub trait gather_sessionQueryTableAccess {
    #[allow(non_snake_case)]
    /// Get a query builder for the table `GatherSession`.
    fn gather_session(&self) -> __sdk::__query_builder::Table<GatherSession>;
}

impl gather_sessionQueryTableAccess for __sdk::QueryTableAccessor {
    fn gather_session(&self) -> __sdk::__query_builder::Table<GatherSession> {
        __sdk::__query_builder::Table::new("gather_session")
    }
}
