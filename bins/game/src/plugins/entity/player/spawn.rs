//! Player spawn definition (shared data).
//!
//! Re-exported from `bevymmo_shared`: `PLAYER_SPAWN_POINT` and the
//! `EntityDefinition` impl moved to the shared crate.

pub use bevymmo_shared::entity::player::spawn::*;
