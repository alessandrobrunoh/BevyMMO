//! Layered configuration (shared data).
//!
//! Re-exported from `bevymmo_shared`: `Settings` moved to the shared crate so
//! every binary (game, future editor) loads the same configuration.

pub use bevymmo_shared::settings::*;
