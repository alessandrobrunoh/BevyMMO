//! Spell trait and cast context (shared data).
//!
//! Re-exported from `bevymmo_shared`: `Spell`, `SpellCastContext`,
//! `SpellConfig` and related enums moved to the shared crate.

pub use bevymmo_shared::spells::context::*;
