# Development environment: the server runs locally with docker compose.
# Secrets (DB password) go into config/local.toml (gitignored).

database_url = "postgresql://bevy:bevy@127.0.0.1:5433/bevy_mmo"

[server]
bind_addr = "0.0.0.0:5051"

[client]
server_addr = "127.0.0.1:5051"
