//! Entity definition contract (shared data).
//!
//! Re-exported from `bevymmo_shared`: `EntityDefinition` moved to the shared
//! crate so both the server spawn helper and the editor can use it.

pub use bevymmo_shared::entity::definition::*;
