[package]
name = "bevymmo_network"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
bevymmo_gameplay = { path = "../gameplay", features = ["bevy"] }
bevy = { workspace = true, features = ["bevy_window", "serialize"] }
lightyear = { workspace = true, features = ["interpolation", "prediction", "replication", "input_native"] }
serde = { workspace = true }

[lints]
workspace = true
