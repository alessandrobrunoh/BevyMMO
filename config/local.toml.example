# Copy this file to `config/local.toml` to override locally any
# default/development/production value without committing secrets.
#
# `local.toml` is in .gitignore: put passwords, DB URLs with real
# credentials here, and any local tweak.

# Esempio:
# database_url = "postgresql://user:password@host:port/db"
