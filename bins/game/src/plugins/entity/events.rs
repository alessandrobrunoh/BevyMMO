//! Shared entity lifecycle events (shared data).
//!
//! Re-exported from `bevymmo_shared`: `DeathEvent` and `RespawnedEvent`
//! moved to the shared crate.

pub use bevymmo_shared::entity::events::*;
