//! Shared spawn helpers (shared data).
//!
//! Re-exported from `bevymmo_shared`: `GameEntityBundle` and
//! `spawn_entity::<T>()` moved to the shared crate.

pub use bevymmo_shared::entity::spawn::*;
