//! Enemy-specific components (shared data).
//!
//! Re-exported from `bevymmo_shared`: `Enemy`, `AggroRange`, `Respawning` and
//! `ENEMY_RESPAWN_SECONDS` moved to the shared crate.

pub use bevymmo_shared::entity::enemy::components::*;
