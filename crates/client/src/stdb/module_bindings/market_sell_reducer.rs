// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct MarketSellArgs {
    pub npc_entity_id: u64,
    pub instance_id: u64,
    pub min_price: u64,
    pub quantity: u32,
}

impl From<MarketSellArgs> for super::Reducer {
    fn from(args: MarketSellArgs) -> Self {
        Self::MarketSell {
            npc_entity_id: args.npc_entity_id,
            instance_id: args.instance_id,
            min_price: args.min_price,
            quantity: args.quantity,
        }
    }
}

impl __sdk::InModule for MarketSellArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `market_sell`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait market_sell {
    /// Request that the remote module invoke the reducer `market_sell` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`market_sell:market_sell_then`] to run a callback after the reducer completes.
    fn market_sell(
        &self,
        npc_entity_id: u64,
        instance_id: u64,
        min_price: u64,
        quantity: u32,
    ) -> __sdk::Result<()> {
        self.market_sell_then(npc_entity_id, instance_id, min_price, quantity, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `market_sell` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn market_sell_then(
        &self,
        npc_entity_id: u64,
        instance_id: u64,
        min_price: u64,
        quantity: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl market_sell for super::RemoteReducers {
    fn market_sell_then(
        &self,
        npc_entity_id: u64,
        instance_id: u64,
        min_price: u64,
        quantity: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            MarketSellArgs {
                npc_entity_id,
                instance_id,
                min_price,
                quantity,
            },
            callback,
        )
    }
}
