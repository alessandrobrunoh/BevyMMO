//! Spell cast events (shared data).
//!
//! Re-exported from `bevymmo_shared`: `SpellCastRequest` and
//! `SpellReleaseRequest` moved to the shared crate.

pub use bevymmo_shared::spells::events::*;
