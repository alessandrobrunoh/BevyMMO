//! Shared entity components (shared data).
//!
//! Re-exported from `bevymmo_shared`: `GameEntity`, `EntityState`,
//! `SpawnPoint`, `PlayerName` and `EntityKind` moved to the shared crate.

pub use bevymmo_shared::entity::components::*;
