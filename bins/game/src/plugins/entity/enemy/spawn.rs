//! Enemy spawn definition (shared data).
//!
//! Re-exported from `bevymmo_shared`: the `EntityDefinition` impl for `Enemy`
//! moved to the shared crate.

pub use bevymmo_shared::entity::enemy::spawn::*;
