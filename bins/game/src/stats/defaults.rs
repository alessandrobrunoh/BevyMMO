//! Default stat profiles (shared data).
//!
//! Re-exported from `bevymmo_shared`: player/enemy/boss/dummy defaults moved
//! to the shared crate so persistence and spawn use the same values.

pub use bevymmo_shared::stats::defaults::*;
