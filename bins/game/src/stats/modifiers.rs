//! Modifier runtime types (shared data).
//!
//! Re-exported from `bevymmo_shared`: `ActiveStatModifiers` and its
//! supporting types moved to the shared crate.

pub use bevymmo_shared::stats::modifiers::*;
